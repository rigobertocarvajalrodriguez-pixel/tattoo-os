PORT=3000
ADMIN_EMAIL=rigobertocarvajalrodriguez@gmail.com
# Usa una contraseña fuerte y única aquí - nunca dejes el valor de ejemplo en producción.
ADMIN_PASS=change-me-to-a-strong-unique-password
NODE_ENV=production

# Cloud database (Render / Supabase) — takes priority over individual vars below
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Local development (only used when DATABASE_URL is not set)
PG_HOST=localhost
PG_PORT=5432
PG_DB=tattoo_os
PG_USER=postgres
PG_PASS=tattoo123
